笔记
==================================================
圆
==================================================
	sin
		Math.sin();
	cos
		Math.cos();

		注意:里面放的都是弧度

	需求：把角度转成弧度
	360° = 2π
	180° = π

	1° = π/180(弧度)

	角度-》弧度
	n° = n*π/180

	弧度-》角度
	n弧 = n*180/π


	π
		Math.PI

	一圈360° 		有N个球
		1个球： 	360/N

		i球： 		360/N*i
=======================================================
穿墙
=======================================================
	a = clientX-oDiv.offsetLeft-R;
	b = oDiv.offsetTop+r-clientY;

	Math.atan2(b,a);
=======================================================
linear 	ease-in 	ease-out
move.js
doMove.js
	tween算法


	http://www.itstrive.com/

========================================================
var arr = [1,2,3];
	arr.unshift(arr.pop());
		  [3,1,2]
var arr = [1,2,3];
	arr.push(arr.shift());
		  [2,3,1]


3D图片轮换：
	保存所有样式和事件
	交换数组中的位置
	重新赋值
=====================================================
使用速度做运动
弹性运动
	iSpeed += (300-left)/5;


	iSpeed -= (left-300)/5;
	iSpeed += -(left-300)/5;
	iSpeed += (300-left)/5;
	弹性菜单
碰撞

	碰撞+拖拽
==============================================================